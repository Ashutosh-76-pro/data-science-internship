# Datasets

## Primary dataset
`sales_data.csv` is a synthetic retail-sales dataset created for the Data Analyst prerequisite.

Columns include transaction ID, order date, region, segment, category, product, units, price, cost, discount and return status.

It intentionally contains data-quality issues (a missing discount and a duplicate transaction ID) so the cleaning workflow can be demonstrated.
